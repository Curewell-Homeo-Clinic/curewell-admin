/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["avatars.githubusercontent.com", "www.functionofbeauty.com"],
  },
};

module.exports = nextConfig;
